name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-2022
    steps:
      # Prepare
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: gittools/actions/gitversion/setup@v0.10.2
        with:
          versionSpec: '5.12.x'
      - uses: gittools/actions/gitversion/execute@v0.10.2
        id: gitversion

      # Build
      - name: Build
        run: .\build.ps1 ${{steps.gitversion.outputs.nuGetVersion}}
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          path: 0publish-win-${{steps.gitversion.outputs.nuGetVersion}}.*

      # Release
      - name: Create GitHub Release
        if: steps.gitversion.outputs.preReleaseLabel == ''
        uses: softprops/action-gh-release@v1
        with:
          files: 0publish-win-*.*
          body: |
            **[Documentation and download instructions](https://docs.0install.net/tools/0publish-win/)**

            Based on [Zero Install .NET ${{steps.gitversion.outputs.nuGetVersion}}](https://github.com/0install/0install-dotnet/releases/${{steps.gitversion.outputs.nuGetVersion}}).
      - name: Publish feed
        if: steps.gitversion.outputs.preReleaseLabel == ''
        env:
          GH_TOKEN: ${{secrets.PERSONAL_TOKEN}}
        run: >
          gh workflow run --repo=0install/apps Incoming
          -f feed_url=https://github.com/${{github.repository}}/releases/download/${{steps.gitversion.outputs.nuGetVersion}}/0publish-win-${{steps.gitversion.outputs.nuGetVersion}}.xml
          -f archive_url=https://github.com/${{github.repository}}/releases/download/${{steps.gitversion.outputs.nuGetVersion}}/0publish-win-${{steps.gitversion.outputs.nuGetVersion}}.tar.zst
